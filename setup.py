from setuptools import setup, find_packages

setup(
    name='src',
    version='0.0.1',
    author='Gavis',
    author_email='gavis@gmail.com',
    packages=find_packages(), # this will find all the packages in the src folder
)